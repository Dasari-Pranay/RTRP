// This function will handle the booking process for each trip
function bookTrip(type) {
    if (type === 'Average') {
        alert('You have successfully booked the Average Trip for 20K-25K');
    } else if (type === 'Premium') {
        alert('You have successfully booked the Premium Trip for 40K-50K');
    }
}